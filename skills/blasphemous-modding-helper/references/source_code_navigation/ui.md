# UI

List of important UI code components in Blasphemous source code.

---

## Gameplay.UI.UIController

Class that handles all the UI components in game. Contains a lot of useful methods and properties that directly control in-game UI. Refer to its active instance (singleton) by `UIController.instance`